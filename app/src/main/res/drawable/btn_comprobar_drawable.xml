<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/color_botones" /> <!-- Color de fondo del botón -->
    <stroke
        android:width="2dp"
        android:color="@android:color/white" /> <!-- Borde blanco -->
    <corners android:radius="8dp" /> <!-- Esquinas redondeadas opcionales -->
</shape>