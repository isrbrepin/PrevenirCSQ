<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PrevenirCSQ" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="colorPrimary">@color/white</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/color_botones</item>

        <item name="windowSplashScreenAnimatedIcon">@drawable/image_carga2</item>

        <item name="postSplashScreenTheme">@style/Theme.PrevenirCSQ</item>
    </style>
</resources>